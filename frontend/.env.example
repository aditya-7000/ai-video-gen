# Frontend environment variables
# Copy to .env and set VITE_API_BASE to your backend base URL (no trailing slash)
# Examples:
#   Local dev (proxy to local Flask):
#   VITE_API_BASE=http://localhost:5000
#   Cloud Run / Render / Railway backend:
#   VITE_API_BASE=https://your-backend.example.com

VITE_API_BASE=
